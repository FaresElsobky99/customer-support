// Used by `ng serve` / `npm start` (the `development` build configuration).
// Point this at your local backend: `uv run uvicorn backend.app.api_main:app --reload`.
export const environment = {
  apiUrl: 'http://localhost:8000',
};
